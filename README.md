# FieldMetrik

FieldMetrik is a construction employee management workspace for tracking crews, sites, attendance, daily hours, salary exposure, and payment history.

## Getting Started

Install dependencies and start the local development server:

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) to use the app.

## Available Scripts

```bash
npm run dev
npm run build
npm run start
npm run lint
```

## Notes

This rebrand is user-facing only. The local folder, Git repository, and package identity are intentionally unchanged.
